# Force text files to use LF (Unix-style) line endings
* text=auto

# Treat these files as binary (no diffs, no merges)
*.png binary
*.jpg binary
*.gif binary
*.pdf binary
*.zip binary
*.exe binary

# Handle Markdown and JSON files as text with UTF-8 encoding
*.md text
*.json text

# Set diff rules for specific file types
*.js diff=javascript
*.css diff=css
*.html diff=html
*.py diff=python
*.rb diff=ruby

# Use linguist to detect languages in a multi-language project
# Example: treat all .md files as documentation, even if large
*.md linguist-documentation

# For large files or files that don’t need diffing
*.log -diff
*.min.js -diff
*.min.css -diff

# Handle files generated by build processes (e.g., don't diff)
*.map -diff
*.class -diff

# Treat shell scripts as text and enforce LF line endings
*.sh text eol=lf

# Handle Windows and Unix executables properly (respect OS)
*.sh eol=lf
*.bat eol=crlf

# Force all files in these directories to be treated as binary
lib/**/* binary
vendor/**/* binary
